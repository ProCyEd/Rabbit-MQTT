import sqlite3
import sys

def getAllBoxes():
    print("heeeee")
    # conn = sqlite3.connect('IoT_Boxes.db')

    # c = conn.cursor()

    # c.execute("SELECT * FROM  boxes")

    # items = c.fetchall()

    # conn.commit()
    # conn.close()
    # return items
